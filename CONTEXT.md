# Context

Token 消耗统计插件（修仙主题）的领域词汇表。本文件只收录领域词汇，不含实现细节。

## 词汇

- **修为**（Cultivation）：全局累计的 token 消耗量，等于输入 + 输出 + 缓存三类 token 之和，只增不减。它是驱动境界进阶的唯一数值。
- **境界**（Realm）：由修为区间划定的九阶阶段，从低到高为：炼气 → 筑基 → 金丹 → 元婴 → 化神 → 炼虚 → 合体 → 大乘 → 渡劫。
- **子层次**（Sub-realm）：境界内部的细分档位。炼气分十二层；筑基分初、中、后期；金丹至渡劫各分初、中、后期。共 36 档。
- **称号**（Title）：头顶显示的文字，格式为「境界名 + 子层次」，如「炼气三层」「筑基中期」「金丹后期」。
- **突破**（Breakthrough）：修为跨越某个大境界边界时触发的瞬态变身事件。一次跨越多个大境界时只播放一次动画并直达最终境界。
- **静止**（Idle）：没有 token 产出时的状态。
- **工作**（Working）：模型正在流式产出 token 时的状态。
- **修为档案**（Cultivation Ledger）：持久化累计修为值的记录，跨进程重启后据此续算。
